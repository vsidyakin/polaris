/* Build-time markup helpers for /platform/tco.
   Extracted from the v1.95 POC page renderer. */
/* eslint-disable */
// @ts-nocheck
import { CALCDATA } from "../../data/tco";
import { ph, phBlock, opts } from "../blocks";

export const oem0=Object.keys(CALCDATA.comp)[0];
